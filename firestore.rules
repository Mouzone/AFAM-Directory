rules_version = '2';
service cloud.firestore {
    match /databases/{database}/documents {
        match /users/{documents=**} {
            allow read: if request.auth != null;
        }
        match /directories/{directoryId}/people/{documents=**} {
            allow read: if request.auth != null;
        }
    }
}