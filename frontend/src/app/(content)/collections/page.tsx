export default function Page({}) {
    // bounce if not verified

    // fetch all collections the user owns
    // then render name, ownner, update date in a card style component
    return <></>;
}
